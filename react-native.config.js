module.exports = {
  project: {
    ios: {},
    android: {},
  },
  dependencies: {
    'react-native-vector-icons': {
      platforms: {
        ios: null, // We'll handle this manually if needed
        android: null, // We'll handle this manually if needed
      },
    },
  },
};
