<?xml version="1.0" encoding="utf-8"?>
<!--
    Network Security Configuration for Sisters Promise Mobile App

    This configuration enforces HTTPS for all network connections,
    which is required for Google Play Store compliance.

    Reference: https://developer.android.com/training/articles/security-config
-->
<network-security-config>
    <!-- Base configuration: Only allow HTTPS connections -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <!-- Trust system certificate authorities -->
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Debug overrides: Allow localhost connections for development -->
    <debug-overrides>
        <trust-anchors>
            <!-- Trust user-added CA certificates for debugging -->
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
